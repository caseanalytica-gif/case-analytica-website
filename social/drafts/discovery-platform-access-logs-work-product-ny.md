Most discovery today isn't a manila folder, it's a login. What almost nobody flags for a client is that the platform is also keeping a log of exactly what the defense opened, how many times, and for how long.

https://www.caseanalytica.com/articles/discovery-platform-access-logs-work-product-ny.html

Note: the bare URL above is intentional, for LinkedIn. Pasting the raw link on its own line lets LinkedIn auto-generate a clickable preview card. Do not attach assets/social/discovery-platform-access-logs-work-product-ny-og.png (or any image) to the LinkedIn post itself, an uploaded photo can't be a clickable link, only the bare URL creates the card. The graphic is for Instagram, Facebook, and X instead, where an uploaded image works as the post.

Graphic: NOT generated this run. The Canva MCP tool is unavailable in this session (requires authentication the account owner hasn't completed yet). A real designed graphic already exists at assets/social/discovery-platform-access-logs-work-product-ny-og.png, and the article's own og:image tag already points at it, so reuse that file.
